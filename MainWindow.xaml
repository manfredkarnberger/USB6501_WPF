<Window x:Class="USB6501_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:USB6501_WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0 5">
            <TextBlock Text="High-Zeit (ms):" VerticalAlignment="Center" Width="100"/>
            <TextBox x:Name="HighTimeTextBox" Width="100" Text="500"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0 5">
            <TextBlock Text="Low-Zeit (ms):" VerticalAlignment="Center" Width="100"/>
            <TextBox x:Name="LowTimeTextBox" Width="100" Text="500"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0 5">
            <TextBlock Text="Wiederholungen (0 = unendlich):" VerticalAlignment="Center" Width="200"/>
            <TextBox x:Name="RepeatCountTextBox" Width="80" Text="0"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="0 10" HorizontalAlignment="Center">
            <Button Content="Start" Width="80" Margin="10,0" Click="StartButton_Click"/>
            <Button Content="Stop" Width="80" Margin="10,0" Click="StopButton_Click"/>
        </StackPanel>
        
    </Grid>
</Window>
